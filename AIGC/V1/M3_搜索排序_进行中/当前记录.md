# M3 搜索排序当前记录

## 当前目标

在本地索引基础上接入真实搜索结果，并完成键盘闭环。

## 本轮范围

1. 搜索输入归一化
2. 拼音全拼与首字母生成
3. 规则评分器
4. 主浮层显示真实结果列表
5. 上下键切换
6. Enter 启动选中项
7. 空输入显示最近使用或推荐项

## 验收标准

- `wx` 可命中常见中文应用
- `weixin` 可命中微信
- `term` 可命中 Windows Terminal
- 键盘可完成选中和启动

