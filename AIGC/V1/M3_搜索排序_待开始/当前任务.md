# M3 搜索排序当前任务

## 当前目标

在已有本地索引基础上，接入名称、拼音和模糊排序，让搜索框返回真正可用的结果列表。

## 本阶段任务

1. 建立搜索输入归一化
2. 生成中文、英文、拼音索引字段
3. 实现前缀 / 子串 / 拼音 / 首字母匹配
4. 接入简单模糊评分器
5. 主浮层显示真实结果列表
6. 上下键选择和 Enter 启动首项

## 完成标准

- `wx` 可命中微信
- `weixin` 可命中微信
- `term` 可命中 Windows Terminal
- 搜索结果可用键盘选择

