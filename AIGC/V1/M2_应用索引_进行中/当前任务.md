# M2 应用索引当前任务

## 当前目标

把应用发现和本地缓存打通，避免后续搜索阶段每次都直接扫系统。

## 本阶段任务

1. 建立统一 `AppEntry`
2. 扫描开始菜单 `.lnk`
3. 扫描桌面 `.lnk`
4. 枚举 `UWP`
5. 扫描自定义目录 `.exe`
6. 去重与噪音过滤
7. 写入本地索引缓存
8. 在设置页显示索引状态并支持重建

## 完成标准

- 本地可生成索引缓存
- 设置页能看到索引条目数和更新时间
- 可手动触发重建索引
- 编译通过

