# M4 启动与体验阶段记录

## 本阶段目标
- 补齐空输入推荐和结果展示
- 完成鼠标启动与启动后行为收口
- 修正设置页文本和交互提示
- 完成 V1 构建验证

## 本阶段完成项
- 主浮层结果项增加来源说明和图标占位文字
- 支持鼠标单击启动应用
- 支持“启动应用后自动隐藏”设置即时生效
- 空输入时显示最近使用和推荐结果
- 设置页全部文案恢复为可读中文
- 搜索服务整理为 `AppSearchService`，避免旧实现继续污染 V1 收口
- 主界面默认收缩为仅搜索栏，输入后再展开推荐列表
- 设置入口改为搜索栏和结果项右键菜单，移除底部提示区
- 补齐“鸣潮”等缺失中文字符的拼音映射，`min` 可命中 `mingchao`
- 搜索结果优先显示应用真实图标，失败时退回文字占位
- 主搜索浮层升级为半透明悬浮卡片风格，搜索栏和结果列表层级更清晰
- 设置页统一为更精致的卡片化视觉样式，和主浮层保持同一套设计语言

## 验证结果
- `dotnet build QuickLauncher.sln -p:BaseIntermediateOutputPath=D:\UnityDemo\QuickLauncher\_build\obj\ -p:BaseOutputPath=D:\UnityDemo\QuickLauncher\_build\bin\`
- 结果：`0 warning / 0 error`

## 阶段结论
- `M4` 已完成
- `V1` 已达到当前计划范围内的完整交付标准
